# 注意: 此 workflow 当前禁用,因为 en/ 目录需要保持上游原样
# 如需构建英文版,请先从上游同步后再启用
# 
# 当前建议: 只使用 build-zh-cn.yml 构建中文版

name: CI - Build English Version (DISABLED)

on:
  # 暂时禁用自动触发
  # push:
  #   branches:
  #     - master
  workflow_dispatch:  # 只允许手动触发

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show notice
        run: |
          echo "================================================"
          echo "注意: 构建英文版前,请确保 en/ 目录与上游同步"
          echo "Upstream: https://github.com/swaroopch/byte-of-python"
          echo "================================================"
          echo ""
          echo "如需构建英文版,请:"
          echo "1. 本地运行: git checkout upstream/master -- en/"
          echo "2. 提交更改"
          echo "3. 然后重新运行此 workflow"
          echo "================================================"

      # 以下步骤暂时注释掉
      # - name: Install Calibre
      #   run: |
      #     sudo apt update
      #     sudo apt install calibre
      #
      # - name: Install Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #
      # - name: Build English version
      #   run: |
      #     npm install honkit --save-dev
      #     npx honkit build ./en ./public --log=debug
